Reference images (Step 2 of the master prompt) would live here:

    MLX-hero-reference.png       — dam/reservoir at dusk with subtle data-signal overlay
    MLX-signal-reference.png     — macro abstract data-light detail in accent color
    MLX-environment-reference.png— wide energy/grid infrastructure at golden/blue hour

Not generated in this build (no image-gen tooling available in this environment).
